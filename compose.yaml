services:
  authorization-db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: authorization
      POSTGRES_USER: authorization
      POSTGRES_PASSWORD: authorization
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U authorization -d authorization"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - authorization-db-data:/var/lib/postgresql/data

  keycloak:
    image: quay.io/keycloak/keycloak:26.4
    command: start-dev --import-realm
    environment:
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
    ports:
      - "8080:8080"
    volumes:
      - ./infra/keycloak/health-insurance-realm.json:/opt/keycloak/data/import/health-insurance-realm.json:ro

  authorization-service:
    build: services/authorization-service
    environment:
      DB_URL: jdbc:postgresql://authorization-db:5432/authorization
      DB_USERNAME: authorization
      DB_PASSWORD: authorization
      KEYCLOAK_ISSUER_URI: http://keycloak:8080/realms/health-insurance
      KEYCLOAK_JWK_SET_URI: http://keycloak:8080/realms/health-insurance/protocol/openid-connect/certs
    ports:
      - "8081:8081"
    depends_on:
      authorization-db:
        condition: service_healthy
      keycloak:
        condition: service_started

volumes:
  authorization-db-data:
